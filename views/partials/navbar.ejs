<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="/img/logo.png" alt="EuColecionoCards" width="80" class="d-inline-block align-text-top">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navMenu">
            <ul class="navbar-nav">
                <li class="nav-item"><a class="nav-link text-white" href="/">Início</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="/cartas">Cartas</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="/sobre">Sobre</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="/contato">Contato</a></li>
                <% if (role==='admin' ) { %>
                    <li class="nav-item"><a class="nav-link text-white" href="/administradores">Painel Admin</a></li>
                    <% } else if (role==='user' ) { %>
                        <li class="nav-item"><a class="nav-link text-white" href="/usuario">Minha Área</a></li>
                        <% } %>
            </ul>
            <ul class="navbar-nav align-items-center">
                <li class="nav-item me-2">
                    <a href="/carrinho" class="nav-link text-white d-flex align-items-center gap-2">
                        <i class="bi bi-cart-fill fs-5"></i>
                        <span id="cart-count" class="badge rounded-pill bg-danger"
                            style="white-space:nowrap;display:none"></span>
                    </a>
                </li>
                <% if (username) { %>
                    <li class="nav-item text-white-50 d-flex align-items-center me-2">
                        <i class="bi bi-person-circle me-1 fs-5"></i>
                        <span class="small">
                            <%= username %>
                        </span>
                    </li>
                    <li class="nav-item">
                        <form method="POST" action="/logout" class="d-inline">
                            <input type="hidden" name="_csrf" value="<%= csrf %>">
                            <button type="submit" class="btn btn-outline-light btn-sm">
                                <i class="bi bi-box-arrow-right me-1"></i>Sair
                            </button>
                        </form>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a href="/login" class="btn btn-outline-light btn-sm">
                                <i class="bi bi-person fs-5 me-1"></i>Entrar
                            </a>
                        </li>
                        <% } %>
            </ul>
        </div>
    </div>
</nav>